{
  "name": "sna-marketing-auditor",
  "version": "1.0.0",
  "description": "SNA Forensic Marketing Auditor: ingests Meta Ads Manager CSV exports and emits a dynamic HTML audit dashboard.",
  "private": true,
  "scripts": {
    "build": "tsc -p . && node ./engine/scripts/postbuild.js",
    "audit": "node dist/engine/index.js",
    "clean": "rm -rf dist",
    "postbuild": "node ./engine/scripts/postbuild.js"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/papaparse": "^5.3.14",
    "typescript": "^5.4.5"
  }
}